describe('This is the first desc', () => {
    it('the it for the first', () => {
        expect(1).toEqual(1)
    });
});

describe('This is the second desc', () => {
    it('the it for the second', () => {
        expect(1).toEqual(1)
    });
});